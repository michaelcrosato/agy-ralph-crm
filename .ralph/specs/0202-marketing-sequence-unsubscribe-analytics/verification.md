# Specification: Marketing Sequence Unsubscribe Analytics - Verification

## 1. Verification Scripts
Execute the following commands from the root directory `C:\dev\agy-ralph-crm`:

```bash
# Typecheck, Lint and Build the workspace
pnpm verify

# Run the integration test suite
pnpm test
```
